# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=game_store

# Server Configuration
PORT=3000

# Database Reset (DANGER: drops and recreates the entire database)
# Remove or set to false after first restart to avoid wiping data every time
DB_RESET=true
